# LiveBench Requirements

# Core dependencies
fastapi>=0.104.0
uvicorn>=0.24.0
websockets>=12.0

# MCP support (required for tools)
fastmcp>=0.2.0

# LangChain and OpenAI
langchain>=0.1.0
langchain-openai>=0.0.2
langchain-mcp-adapters>=0.1.0
langgraph>=0.2.0
openai>=1.0.0

# Data handling
pandas>=2.0.0
pyarrow>=14.0.0

# Environment
python-dotenv>=1.0.0

# HTTP requests
requests>=2.31.0

# Web search APIs
tavily-python>=0.3.0  # Tavily search (recommended)

# Utilities
aiofiles>=23.2.1

python-docx>=1.0.0
python-pptx>=0.6.21
reportlab>=4.0.0
openpyxl>=3.1.0
xlsxwriter>=3.1.0
pdf2image>=1.16.0
Pillow>=10.0.0

# Revenue Streams — Scheduler
apscheduler>=3.10.0  # Cron scheduler for autonomous runs

# Revenue Streams — Platform Publishers (install only what you use)
tweepy>=4.14.0        # Twitter/X publishing
praw>=7.7.0           # Reddit publishing
markdown>=3.5.0       # Markdown → HTML for Medium/WordPress publishers
# Note: LinkedIn, Medium, WordPress, Beehiiv, Buffer use requests (already installed)